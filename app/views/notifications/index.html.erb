<div class="container">
	<h2>通知</h2>

	<% if @notifications.exists? %>
	    <div class="n-box">
	        <%= render @notifications %>
	    </div>
	    <div class="n-paginate">
		    <%= paginate @notifications %>
		  </div>
	<% else %>
	    <p>通知はありません</p>
	<% end %>
</div>