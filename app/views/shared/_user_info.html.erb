<div class="user-info-box hidden-xs">
	<div class="text-center">
		<%= link_to user_genres_path(user) do %>
			<%= attachment_image_tag user, :image, :fill, 150, 150, class: "prof-img", fallback: "no_image.png", size: "150x150" %>
		<% end %>
	</div>
	<table class="table">
	    <tbody>
	      <tr></tr>
	      <tr>
	         <td>
	         	<div class="text-center">
		            <strong><%= user.name %></strong>
		         </div>
	         </td>
	      </tr>
	      <tr>
	         <td>
	            <p class="intro-title"><strong>自己紹介</strong></p>
	            <p class="text-left"><%= user.introduction %></p>
	         </td>
	      </tr>
	      <tr>
	      	<td>
	      		<%= render 'shared/stats', user: user %>
	      	</td>
	      </tr>
	      <% if user_signed_in? %>
	         <% if current_user != user %>
	            <tr>
	               <td>
	                  <div class="text-center">
	              	      <%= render 'users/follow_form', user: user %>
	                  </div>
	               </td>
	            </tr>
	         <% else %>
	            <tr>
	               <td>
	                  <div class="text-center">
	                     <%= link_to "記事を書く", new_article_path ,class: "btn btn-info" %>
	                  </div>
	               </td>
	            </tr>
	         <% end %>
	         <tr>
               <td>
            	<!-- カレンダー表示 -->
            	   <div class="text-center">
                     <%= link_to "カレンダーを見る", calendar_user_path(user) ,class: "btn btn-success" %>
                  </div>
               </td>
            </tr>
	      <% end %>
	   </tbody>
	</table>
</div>


